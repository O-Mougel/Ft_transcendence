services:
  nginx:
    build: docker/nginx/.
    env_file: ../.env
    container_name: nginx
    depends_on:
     - front
     - back
    ports:
<<<<<<< HEAD
     - "4430:443"
    volumes:
     - front:/nginx
=======
      - "8443:8443"
        #    volumes:
        #      - front:/
>>>>>>> pong
    networks:
     - transcendence
    restart: on-failure

  front:
    build:
      context: .
      dockerfile: docker/front/Dockerfile
    env_file: ../.env
    container_name: front
    depends_on:
     - back
    volumes:
     - front:/usr/src/app
    networks:
     - transcendence
    restart: on-failure
     #    expose:
     #      - ""

  back:
    build: docker/back/.
    env_file: ../.env
    container_name: back 
    volumes:
      - back:/home
    networks:
      - transcendence
    restart: on-failure
    ports:
      - "8080:3001"

networks:
  transcendence:
    driver: bridge

volumes:
  back:
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
      device: 'back/server'

  front:
    driver: local
    driver_opts:
      type: 'none' 
      o: 'bind'
      device: 'front/'
